{
	"name": "opsiweb",
	"dockerComposeFile": ["./docker-compose.yml"],
	"service": "opsiweb_dev",
	"runServices": ["opsiweb_dev", "opsi-server", "mysql", "redis", "grafana"],
	"workspaceFolder": "/workspace",
	"postCreateCommand": "/workspace/.devcontainer/post_create.sh",
	// "initializeCommand": "docker volume rm opsiweb-ui_devcontainer_opsi_data",
	// "forwardPorts": [
	// 	"opsiweb_dev:8888",
	// 	"opsi-server:4447",
	// 	// "opsiweb_dev:${localEnv:WEBGUI_DEV_PORT}",
	// 	// "opsi-server:${env:OPSICONFD_PORT}",
	// ],
	// "portsAttributes": {
	// 	// "opsiweb_dev:${env:WEBGUI_DEV_PORT}": {
	// 	"opsiweb_dev:8888": { "label": "webgui" },
	// 	"opsi-server:4447": { "label": "opsiserver" }
	// },
	// "otherPortsAttributes": { "onAutoForward" : "notify" },
	"settings": { "terminal.integrated.shell.linux": "/usr/bin/zsh" },
	"customizations": {
		"vscode": {
			// cleaning up. only install the ones which are really needed/useful
			"extensions": [
				////////// Vue/Nuxt3
				// "octref.vetur",
				"Vue.volar",
				// "Vue.vscode-typescript-vue-plugin",
				// "misterj.vue-volar-extention-pack",
				"wejectchan.vue3-snippets-for-vscode",
				// "johnsoncodehk.vscode-typescript-vue-plugin",

				////////// helpers for other libs
				"lokalise.i18n-ally",
				"bradlc.vscode-tailwindcss",
				"antfu.iconify",
				// "orta.vscode-jest",
				// "ms-playwright.playwright",
				// "sakamoto66.vscode-playwright-test-runner",

				////////// linter
				// "SonarSource.sonarlint-vscode",
				// "dbaeumer.vscode-eslint",

				////////// container and building app
				"fooxly.workspace",
				"philfontaine.autolaunch",
				// "pucelle.run-on-save",

				////////// helpers
				"gruntfuggly.todo-tree",
				"esbenp.prettier-vscode",
				"shardulm94.trailing-spaces",
				"seunlanlege.action-buttons",
				// "eamodio.gitlens",

			]
		}
	}
}