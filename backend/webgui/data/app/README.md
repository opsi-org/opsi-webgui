# Frontend Build Output

This directory contains the built frontend files from `/workspace/frontend`.

**Do not edit files here directly.** Changes will be overwritten on next build.

## Building Frontend

```bash
cd /workspace/frontend
pnpm build
```

Build output is placed in this directory and served by the webgui addon at:
- https://localhost:4447/addons/webgui/app
