version: '3.3'

services:
  nuxt:
    image: node:16
    volumes:
      - ..:/workspace
    working_dir: /workspace/opsiweb
    # build:
    #   dockerfile: docker/Dockerfile.dev
    entrypoint: ["../docker/docker-entrypoint.sh"]