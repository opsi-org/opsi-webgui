export default {
  title: 'Tree/TS Group Init Selection',
  parameters: { docs: { description: { component: 'Treeselect for group selection' } } }
}

const PrimaryTemplate = () => ({
  template: '<TreeTSGroupInitSelection />'
})

export const TSGroupInitSelection = PrimaryTemplate.bind({})
